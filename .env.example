# ============================================================================
# Data Engineering Project - Environment Configuration
# ============================================================================
# Copy this file to .env and customize values as needed
# All variables are optional and have sensible defaults

# ============================================================================
# Download Settings
# ============================================================================

# Chunk size for streaming downloads (bytes)
# Default: 1048576 (1 MB)
# Range: 1 - 10485760 (10 MB)
# ENV_DOWNLOAD_CHUNK_SIZE=1048576

# Maximum time for entire download (seconds)
# Default: 600 (10 minutes)
# Range: 1 - 3600 (1 hour)
# Note: Must be greater than connect and sock_read timeouts
# ENV_DOWNLOAD_TIMEOUT_TOTAL=600

# Maximum time to establish connection (seconds)
# Default: 10
# Range: 1 - 60
# ENV_DOWNLOAD_TIMEOUT_CONNECT=10

# Maximum time between data packets (seconds)
# Default: 30
# Range: 1 - 300 (5 minutes)
# ENV_DOWNLOAD_TIMEOUT_SOCK_READ=30

# ============================================================================
# Retry Settings (for future use)
# ============================================================================

# Maximum number of retry attempts
# Default: 3
# Range: 1 - 10
# ENV_RETRY_MAX_ATTEMPTS=3

# Initial delay between retries (seconds)
# Default: 1.0
# Range: 0.1 - 60.0
# ENV_RETRY_INITIAL_DELAY=1.0

# Multiplier for delay after each retry
# Default: 2.0 (exponential backoff: 1s, 2s, 4s, 8s, ...)
# Range: 1.0 - 10.0
# ENV_RETRY_BACKOFF_FACTOR=2.0

# ============================================================================
# Hash Settings
# ============================================================================

# Hashing algorithm to use
# Default: sha256
# Allowed values: sha256, sha512, sha1, md5
# Recommended: sha256 (good balance of security and speed)
# ENV_HASH_ALGORITHM=sha256

# Chunk size for file hashing (bytes)
# Default: 131072 (128 KB)
# Range: 1 - 1048576 (1 MB)
# ENV_HASH_CHUNK_SIZE=131072

# ============================================================================
# Example Configurations
# ============================================================================

# Development (fast downloads, short timeouts)
# ENV_DOWNLOAD_TIMEOUT_TOTAL=120
# ENV_RETRY_MAX_ATTEMPTS=1

# Production (robust, longer timeouts)
# ENV_DOWNLOAD_TIMEOUT_TOTAL=3600
# ENV_RETRY_MAX_ATTEMPTS=5
# ENV_RETRY_BACKOFF_FACTOR=3.0

# High-security (stronger hash)
# ENV_HASH_ALGORITHM=sha512

# Performance tuning (larger chunks)
# ENV_DOWNLOAD_CHUNK_SIZE=5242880
# ENV_HASH_CHUNK_SIZE=524288
